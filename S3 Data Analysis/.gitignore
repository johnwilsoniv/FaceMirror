# Build artifacts
build/
dist/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Logs
logs/
*.log

# Cache
.cache/
*.cache

# Test outputs
test_output/

# Analysis results (generated during processing)
analysis_results/*.txt
analysis_results/*.xlsx
analysis_results/*.csv

# Performance data
performance_analysis_results/

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store

# Temporary files
*.tmp
*~

# Backup files
*.backup

# PyInstaller spec files (allow main spec files, ignore build-generated ones)
!Paralysis_Analyzer.spec
!Paralysis_Analyzer_Intel.spec

# Test data files (if not essential)
batch_test.log
combined_results.csv

# Synkinesis artifacts (untrained / private until publish-ready)
models/synkinesis/**/*.pkl
models/synkinesis/**/*.list
models/synkinesis/**/*.csv
!models/synkinesis/**/.gitkeep

# Per-frame AU extraction outputs (production + reruns).
# Large generated artifacts derived from the mp4 corpus, regenerable from
# pyfaceau. Keep out of the repo so a stray `git add .` can't pull them in.
recoded_per_frame/
recoded_rerun_v1315/
recoded_rerun_dual_v1316/
recoded_combined_results.csv
recoded_rerun_dual_v1316_combined_results.csv
hybrid_macS1_winS2_combined_results.csv

# Pilot/ research directory: scripts, PDFs, CSVs, and data dirs are all
# pre-publication research artifacts. Kept local until paper is published.
Pilot/

# Synkinesis investigation working dir (research artifacts, not source)
synkinesis_investigation/

# Cohort data dump (raw video metadata, not source)
RECODED_DATASET.md
