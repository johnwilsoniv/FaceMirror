# Core dependencies
opencv-python>=4.7.0
numpy>=1.24.0
scipy>=1.10.0
tqdm>=4.65.0
psutil>=5.9.0

# OpenFace 3.0 for face detection and landmark tracki
openface-test>=0.1.0
torch>=2.0.0

# UI (usually included with Python, but listed for completeness)
# tkinter comes with Python by default on most systems
